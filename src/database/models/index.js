
const commentModel = require('./commentScheme')
const movieModel = require('./movieScheme')
const userModel = require('./userScheme')

module.exports = {
    commentModel,
    movieModel,
    userModel

}